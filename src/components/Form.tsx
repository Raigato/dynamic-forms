import type { FormData } from '@/types/FormData'

interface Props {
  form: FormData
}

const Form = ({ form }: Props) => {
  //TODO: Implement form component

  return <div>Form</div>
}

export default Form
